name: PR Auto-merge

on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: arc-runner-temple-t
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Run tests
        uses: ./.github/actions/test

  automerge:
    runs-on: arc-runner-temple-t
    needs: test
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: automerge
        uses: pascalgn/automerge-action@v0.16.4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MERGE_LABELS: ""
